// regex solo per numeri e numeri separati da punti
export const REGEX_VERSION = /^(\d+|\d+(\.\d+)+)$/

// regex per estrarre solo numeri o numeri separati da punti
export const REGEX_GET_VERSION = /(?:\d+\.)*\d+/